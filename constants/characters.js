const crtConst = {
  preSetList: ['무지', '라이온', '제이지'], // 캐릭터 풀 같은 고정 상수는 const로 하기 + 다른 파일로 만들어두기 도 좋습니다.
  actionExpGrowth: 20, //임의 값
  routineExpGrowth: 100, //임의 값// -> 임의 값같은 고정값은 다른 폴더에 생성하는 것이 좋습니다.
}

module.exports = crtConst;