//일단 타입 지정하고 js 파일만든 후에, actions, main 컨트롤러에서 사용되는 타임셋 통일하자.

// export function timeSet():object  {
//   const today:Date = new Date();
//   const year:number = today.getFullYear(); // 년도
//   const month:number = today.getMonth(); // 월
//   const day:number = today.getDate(); // 날짜
//   const fromToday:Date = new Date(year, month, day, 0, 0, 0);
//   const fromThisMonth:Date = new Date(year, month, day, 1, 0, 0, 0);
//   const fromYearAgo:Date = new Date(year - 1, month, day, 0, 0, 0);

//   return { fromToday, fromThisMonth, fromYearAgo }
// }
